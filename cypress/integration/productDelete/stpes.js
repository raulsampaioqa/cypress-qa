
Given(/^que estou na tela de cadatrar novos produtos$/, () => {
	return true;
});

Then(/^seleciono a opcao pagamento de contas que desejo cancelar$/, () => {
	return true;
});

When(/^seleciono opcao aprovar novo cadastro de produto$/, () => {
	return true;
});

Then(/^e apresentado mensagem que novo produto foi cancelado$/, () => {
	return true;
});



Given(/^que estou na tela de cadatrar novos produtos$/, () => {
	return true;
});

Then(/^seleciono a opcao credito pessoal que desejo cancelar$/, () => {
	return true;
});

When(/^seleciono opcao aprovar  novo cadastro de nproduto$/, () => {
	return true;
});

Then(/^e apresentado mensagem que novo produto foi cancel$/, () => {
	return true;
});
